<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.7">
  <compounddef id="md___users_bsd-mac__downloads__pruebas_prebuilt-apps-master__field_service__components_sqlite-nea3254d35222426087dee85900d7c36c3" kind="page">
    <compoundname>md__Users_bsd-mac_Downloads_Pruebas_prebuilt-apps-master_FieldService_Components_sqlite-net-1.0.1_component_GettingStarted</compoundname>
    <title>GettingStarted</title>
    <detaileddescription>
<para>Please consult the Wiki for, ahem, <ulink url="https://github.com/praeclarum/sqlite-net/wiki">complete documentation</ulink>.</para><para>The library contains simple attributes that you can use to control the construction of tables. In a simple stock program, you might use:</para><para>```csharp using <ref refid="namespace_s_q_lite" kindref="compound">SQLite</ref>; // ...</para><para>public class Stock { [PrimaryKey, AutoIncrement] public int Id { get; set; } [MaxLength(8)] public string Symbol { get; set; } }</para><para>public class Valuation { [PrimaryKey, AutoIncrement] public int Id { get; set; } [Indexed] public int StockId { get; set; } public DateTime Time { get; set; } public decimal Price { get; set; } } ```</para><para>Once you&apos;ve defined the objects in your model you have a choice of APIs. You can use the &quot;synchronous API&quot; where calls block one at a time, or you can use the &quot;asynchronous API&quot; where calls do not block. You may care to use the asynchronous API for mobile applications in order to increase reponsiveness.</para><para>Both APIs are explained in the two sections below.</para><para><heading level="2">Synchronous API</heading>
</para><para>Once you have defined your entity, you can automatically generate tables in your database by calling <computeroutput>CreateTable</computeroutput>:</para><para>```csharp string folder = Environment.GetFolderPath (Environment.SpecialFolder.Personal); var conn = new SQLiteConnection (System.IO.Path.Combine (folder, &quot;stocks.db&quot;)); conn.CreateTable&lt;Stock&gt;(); conn.CreateTable&lt;Valuation&gt;(); ```</para><para>You can insert rows in the database using <computeroutput>Insert</computeroutput>. If the table contains an auto-incremented primary key, then the value for that key will be available to you after the insert:</para><para>```csharp public static void AddStock (SQLiteConnection db, string symbol) { var s = new Stock { Symbol = symbol }; db.Insert (s); Console.WriteLine (&quot;{0} == {1}&quot;, s.Symbol, s.Id); } ```</para><para>Similar methods exist for <computeroutput>Update</computeroutput> and <computeroutput>Delete</computeroutput>.</para><para>The most straightforward way to query for data is using the <computeroutput>Table</computeroutput> method. This can take predicates for constraining via WHERE clauses and/or adding ORDER BY clauses:</para><para>```csharp var query = conn.Table&lt;Stock&gt;().Where (v =&gt; v.Symbol.StartsWith(&quot;A&quot;));</para><para>foreach (var stock in query) Console.WriteLine (&quot;Stock: &quot; + stock.Symbol); ```</para><para>You can also query the database at a low-level using the <computeroutput>Query</computeroutput> method:</para><para>```csharp public static IEnumerable&lt;Valuation&gt; QueryValuations (SQLiteConnection db, Stock stock) { return db.Query&lt;Valuation&gt; (&quot;select * from Valuation where StockId = ?&quot;, stock.Id); } ```</para><para>The generic parameter to the <computeroutput>Query</computeroutput> method specifies the type of object to create for each row. It can be one of your table classes, or any other class whose public properties match the column returned by the query. For instance, we could rewrite the above query as:</para><para>```csharp public class Val { public decimal Money { get; set; } public DateTime Date { get; set; } }</para><para>public static IEnumerable&lt;Val&gt; QueryVals (SQLiteConnection db, Stock stock) { return db.Query&lt;Val&gt; (&quot;select &apos;Price&apos; as &apos;Money&apos;, &apos;Time&apos; as &apos;Date&apos; from Valuation where StockId = ?&quot;, stock.Id); } ```</para><para>You can perform low-level updates of the database using the <computeroutput>Execute</computeroutput> method.</para><para><heading level="2">Asynchronous API</heading>
</para><para>To use the asynchronous API, you&apos;ll create a <computeroutput>SQLiteAsyncConnection</computeroutput> instead of <computeroutput>SQLiteConnection</computeroutput>. <computeroutput>SQLiteAsyncConnection</computeroutput> exposes asynchronous versions of the database APIs providing you with <computeroutput>Task</computeroutput> to perform continuations on.</para><para>Once you have defined your entity, you can automatically generate tables by calling <computeroutput>CreateTableAsync</computeroutput>:</para><para>```csharp string folder = Environment.GetFolderPath (Environment.SpecialFolder.Personal); var conn = new SQLiteAsyncConnection (System.IO.Path.Combine (folder, &quot;stocks.db&quot;)); conn.CreateTableAsync&lt;Stock&gt;().ContinueWith (t =&gt; { Console.WriteLine (&quot;Table created!&quot;); }); ```</para><para>You can insert rows in the database using <computeroutput>Insert</computeroutput>. If the table contains an auto-incremented primary key, then the value for that key will be available to you after the insert:</para><para>```csharp Stock stock = new Stock { Symbol = &quot;AAPL&quot; };</para><para>conn.InsertAsync (stock).ContinueWith (t =&gt; { Console.WriteLine (&quot;New customer ID: {0}&quot;, stock.Id); }); ```</para><para>Similar methods exist for <computeroutput>UpdateAsync</computeroutput> and <computeroutput>DeleteAsync</computeroutput>.</para><para>Querying for data is most straightforwardly done using the <computeroutput>Table</computeroutput> method. This will return an <computeroutput>AsyncTableQuery</computeroutput> instance back, whereupon you can add predictates for constraining via WHERE clauses and/or adding ORDER BY. The database is not physically touched until one of the special retrieval methods - <computeroutput>ToListAsync</computeroutput>, <computeroutput>FirstAsync</computeroutput>, or <computeroutput>FirstOrDefaultAsync</computeroutput> - is called.</para><para>```csharp var query = conn.Table&lt;Stock&gt;().Where (v =&gt; v.Symbol.StartsWith (&quot;A&quot;));</para><para>query.ToListAsync().ContinueWith (t =&gt; { foreach (var stock in t.Result) Console.WriteLine (&quot;Stock: &quot; + stock.Symbol); }); ```</para><para>There are a number of low-level methods available. You can also query the database directly via the <computeroutput>QueryAsync</computeroutput> method. Over and above the change operations provided by <computeroutput>InsertAsync</computeroutput> etc you can issue <computeroutput>ExecuteAsync</computeroutput> methods to change sets of data directly within the database.</para><para>Another helpful method is <computeroutput>ExecuteScalarAsync</computeroutput>. This allows you to return a scalar value from the database easily:</para><para>```csharp conn.ExecuteScalarAsync&lt;int&gt; (&quot;select count(*) from Stock&quot;, null).ContinueWith (t =&gt; { Console.WriteLine (string.Format(&quot;Found &apos;{0}&apos; stock items.&quot;, t.Result)); }); ``` </para>    </detaileddescription>
  </compounddef>
</doxygen>
